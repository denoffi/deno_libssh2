{
  "tasks": {
    "build": "deno run --allow-read --allow-write --allow-run build.ts && deno fmt",
    "test": "deno test --unstable --allow-ffi"
  },
  "fmt": {
    "options": {
      "lineWidth": 120
    },
    "files": {
      "exclude": ["input/"]
    }
  }
}
